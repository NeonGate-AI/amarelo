# Mobile

Installable React/Vite PWA for Amarelo's voice-first product experience. Handoff 1 intentionally does not refactor React conventions or integrate the new Memory Nucleus runtime; that belongs to later handoffs.

Use the package scripts for local development/build. Canonical behavior lives in `.agents/specs/mobile/` and context in `.agents/context/workflows/`.
