# Protected holdout

The protected holdout is intentionally not stored in this repository. A release evaluation must receive an independently controlled absolute path. If it is unavailable, report `NOT RUN`; never synthesize a replacement and call it independent.
