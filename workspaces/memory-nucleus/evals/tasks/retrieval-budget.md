# Task: bounded retrieval

Given an authorized subject-scoped query and a corpus containing more relevant memories than the budget allows, retrieval must return only eligible memory, stay within the token budget, and report omissions without invoking a model or vector index.
