{
  "name": "amarelo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "sim": "bash ./cli/setup.sh",
    "dev": "turbo dev --filter=landing --filter=console --filter=onboarding",
    "landing": "turbo dev --filter=landing",
    "console": "turbo dev --filter=console",
    "mobile": "turbo dev --filter=mobile",
    "onboarding": "turbo dev --filter=onboarding",
    "landing-start": "turbo start --filter=landing",
    "console-start": "turbo start --filter=console",
    "onboarding-start": "turbo start --filter=onboarding",
    "build": "turbo run build",
    "lint": "biome check apps packages",
    "format": "biome format --write --vcs-use-ignore-file=true apps packages",
    "prepare": "husky cli/husky",
    "reset": "sim clean && pnpm install && pnpm build",
    "postinstall": "husky cli/husky",
    "commitlint": "commitlint --edit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.15",
    "@commitlint/cli": "^20.4.1",
    "@commitlint/config-conventional": "^20.4.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.1",
    "turbo": "^2.8.16",
    "typescript": "5.9.2"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,cjs,mjs,json,jsonc}": [
      "biome lint --write --files-ignore-unknown=true",
      "biome format --write --files-ignore-unknown=true"
    ]
  },
  "packageManager": "pnpm@10.32.1",
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0"
  }
}
