# Workflows context

Workflow implementation is not redesigned in Handoff 1. This directory centralizes engineering context that was previously stored in app/workflow-local agent instruction files. Product workflows remain owned by the runtime/app that executes them.
