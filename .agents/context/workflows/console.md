# Console workflow context

Console is the person's memory review, correction, provenance, permissions and control surface. Example data must remain synthetic. UI copy should use `memória` for the product concept; access/sharing never follows automatically from a support relationship or Elo choice.
