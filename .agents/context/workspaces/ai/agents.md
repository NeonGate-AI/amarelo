# Product agents context

`workspaces/ai/agents/` is reserved for runtime/product AI agents. It is not the engineering `.agents/` harness. Agents may leverage Memory Nucleus via `@repo/memory-sdk` and will later bridge to product workflows. No agent implementation is introduced in Handoff 1.
